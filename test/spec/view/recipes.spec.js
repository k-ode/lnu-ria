/*jshint expr: true*/

define(function (require) {
    'use strict';

    var Backbone = require('backbone'),
        $ = require('jquery'),
        _ = require('underscore'),
        chai = require('chai'),
        sinon = require('sinon'),
        sinonChai = require('sinonChai'),
        RecipesView = '../../../js/view/recipes',
        expect = chai.expect;
    
    chai.use(sinonChai);

    describe("view.recipes", function () {
        
        describe("the initialize function", function () {
            
        });

    });
});
