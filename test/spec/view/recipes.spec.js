/*jshint expr: true*/

define(function (require) {
    'use strict';

    var Backbone = require('backbone'),
        _ = require('underscore'),
        chai = require('chai'),
        RecipesView = '../../../js/view/recipes',
        expect = chai.expect;
    
});
