describe('Recipe model test', function () {
    
    // beforeEach(function (done) {
    //     var self = this;

    //     require([
    //         'underscore',
    //         'backbone',
    //         'module/recipes/model'
    //     ], function (_, Backbone, RecipesModel) {
    //         self._ = _;
    //         self.Backbone = Backbone;
    //         self.RecipesModel = RecipesModel;
    //     });
    // });

    // it('should extend the model', function () {
    //     expect(this.RecipesModel).to.be.a('function');
    // });

    // it('should create correct instances of Backbone Model', function () {
    //     var instance = new this.RecipesModel();
    //     expect(instance).to.be.an.instanceof(this.Backbone.Model);
    // });

    // it('should have correct urlRoot', function () {
    //     var instance = new this.RecipesModel();
    //     expect(instance.urlRoot).to.be.equal('recipes');
    // });

    // it('should fetch correct data', function () {
        
    // })
});
